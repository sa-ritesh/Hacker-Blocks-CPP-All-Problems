#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int sum=0;
    int power=1;
    while(n>0)
    {
        int ld=n%10;
        sum+=(ld*power);
        power*=2;
        n/=10;
    }
    cout<<sum;
}
