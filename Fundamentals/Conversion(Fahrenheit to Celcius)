#include<iostream>
using namespace std;
int main()
{
    int minF, maxF, step;
    cin>>minF>>maxF>>step;
    while(minF<=maxF){
        int ans=(5*(minF - 32))/9;
        cout<<minF<<" "<<ans<<endl;
        minF+=step;
    }
}
